{const rakamlar=[3,4,5,-5,10];
let toplam=0;
for (let i=0;
    i<rakamlar.length;
    i++){
        toplam+=rakamlar[i]
    }
    console.log(toplam);}

    
    /*const dizi=[2,3,5,-5,-10];
    const topla = (dizi)=> {
    let negatifler=0;
    let pozitifler=0;
    for (let i=0;
        i<dizi.length;
        i++);
        if (dizi [i] <0); {
            negatifler += dizi[i];
        } else {
            pozitifler += dizi[i];
        }
        console.log(`dizideki pozitif  sayilarin toplami: ${pozitifler}`);
        console.log(`dizideki negatif sayilarin toplami: ${negatifler}`);
    };
    topla(dizi);*/